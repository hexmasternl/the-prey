name: Users

on:
  push:
    branches: [main]
    paths:
      - 'src/Users/**'
      - 'src/Core/**'
      - 'src/Shared/**'
      - 'src/Aspire/ThePrey.Aspire.ServiceDefaults/**'
      - 'infra/users/**'
      - 'infra/modules/**'
      - '.github/workflows/users.yml'
  workflow_dispatch:
    inputs:
      imageTag:
        description: 'Image tag override (leave blank to use GitVersion semVer)'
        required: false
        default: ''
  pull_request:
    branches: [main]
    paths:
      - 'src/Users/**'
      - 'src/Core/**'
      - 'src/Shared/**'
      - 'src/Aspire/ThePrey.Aspire.ServiceDefaults/**'
      - 'infra/users/**'
      - 'infra/modules/**'
      - '.github/workflows/users.yml'

permissions:
  id-token: write
  contents: read

concurrency:
  group: users-${{ github.ref }}
  cancel-in-progress: false

jobs:
  version:
    name: Version
    runs-on: ubuntu-latest
    outputs:
      semVer: ${{ steps.gitversion.outputs.semVer }}
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Setup GitVersion
        uses: gittools/actions/gitversion/setup@v4.5.0
        with:
          versionSpec: '6.4.x'

      - name: Execute GitVersion
        id: gitversion
        uses: gittools/actions/gitversion/execute@v4.5.0

  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: '10.x'

      - name: Run Users tests
        run: dotnet test src/Users/HexMaster.ThePrey.Users.Tests/HexMaster.ThePrey.Users.Tests.csproj --configuration Release

  publish:
    name: Publish Container
    runs-on: ubuntu-latest
    needs: [version, test]
    if: github.event_name != 'pull_request'
    outputs:
      imageTag: ${{ steps.resolve-tag.outputs.imageTag }}
    steps:
      - uses: actions/checkout@v6

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: '10.0.x'

      - name: Resolve image tag
        id: resolve-tag
        run: |
          TAG="${{ inputs.imageTag }}"
          if [ -z "$TAG" ]; then
            TAG="${{ needs.version.outputs.semVer }}"
          fi
          echo "imageTag=$TAG" >> "$GITHUB_OUTPUT"

      - name: Log in to Azure (ACR)
        uses: azure/login@v3
        with:
          client-id: ${{ secrets.ACR_CLIENT_ID }}
          tenant-id: ${{ secrets.ACR_TENANT_ID }}
          subscription-id: ${{ secrets.ACR_SUBSCRIPTION_ID }}

      - name: Log in to ACR
        run: az acr login --name "${{ secrets.ACR_HOSTNAME }}"

      - name: Publish container
        run: |
          dotnet publish src/Users/HexMaster.ThePrey.Users.Api/HexMaster.ThePrey.Users.Api.csproj \
            --configuration Release \
            --os linux \
            --arch x64 \
            /t:PublishContainer \
            /p:ContainerRegistry=${{ secrets.ACR_HOSTNAME }} \
            /p:ContainerImageTag=${{ steps.resolve-tag.outputs.imageTag }} \
            /p:Version=${{ needs.version.outputs.semVer }} \
            /p:AssemblyVersion=${{ needs.version.outputs.semVer }} \
            /p:InformationalVersion=${{ needs.version.outputs.semVer }}

  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    needs: publish
    if: github.event_name != 'pull_request'
    steps:
      - uses: actions/checkout@v6

      - name: Log in to Azure
        uses: azure/login@v3
        with:
          client-id: ${{ secrets.AZURE_PROD_CLIENTID }}
          tenant-id: ${{ secrets.AZURE_PROD_TENANTID }}
          subscription-id: ${{ secrets.AZURE_PROD_SUBSCRIPTION }}

      - name: Read landing zone outputs
        id: lz
        run: |
          LZ=$(az deployment sub show --name theprey-lz-prod --query properties.outputs -o json)
          echo "appConfigEndpoint=$(echo $LZ | jq -r '.appConfigEndpoint.value')" >> "$GITHUB_OUTPUT"
          AI_CONN=$(echo $LZ | jq -r '.appInsightsConnectionString.value')
          echo "::add-mask::$AI_CONN"
          echo "appInsightsConnectionString=$AI_CONN" >> "$GITHUB_OUTPUT"

      - name: Bicep build (lint)
        run: az bicep build --file infra/users/main.bicep

      - name: Deploy Users
        run: |
          az deployment sub create \
            --name theprey-users-prod \
            --location westeurope \
            --template-file infra/users/main.bicep \
            --parameters infra/users/prod.bicepparam \
            --parameters \
              imageTag="${{ needs.publish.outputs.imageTag }}" \
              registryServer="${{ secrets.ACR_HOSTNAME }}"
