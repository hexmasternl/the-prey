name: PlayFields

on:
  push:
    branches: [main]
    paths:
      - 'src/PlayFields/**'
      - 'src/Core/**'
      - 'src/Shared/**'
      - 'src/Aspire/ThePrey.Aspire.ServiceDefaults/**'
      - 'infra/playfields/**'
      - 'infra/modules/**'
      - '.github/workflows/playfields.yml'
  workflow_dispatch:
    inputs:
      imageTag:
        description: 'Image tag override (leave blank to use GitVersion semVer)'
        required: false
        default: ''
  pull_request:
    branches: [main]
    paths:
      - 'src/PlayFields/**'
      - 'src/Core/**'
      - 'src/Shared/**'
      - 'src/Aspire/ThePrey.Aspire.ServiceDefaults/**'
      - 'infra/playfields/**'
      - 'infra/modules/**'
      - '.github/workflows/playfields.yml'

permissions:
  id-token: write
  contents: read

concurrency:
  group: playfields-${{ github.ref }}
  cancel-in-progress: false

jobs:
  version:
    name: Version
    runs-on: ubuntu-latest
    outputs:
      semVer: ${{ steps.gitversion.outputs.semVer }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup GitVersion
        uses: gittools/actions/gitversion/setup@v4.2.0
        with:
          versionSpec: '6.4.x'

      - name: Execute GitVersion
        id: gitversion
        uses: gittools/actions/gitversion/execute@v4.2.0

  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.x'

      - name: Run PlayFields tests
        run: dotnet test src/PlayFields/HexMaster.ThePrey.PlayFields.Tests/HexMaster.ThePrey.PlayFields.Tests.csproj --configuration Release --no-restore

  docker:
    name: Build & Push
    runs-on: ubuntu-latest
    needs: [version, test]
    if: github.event_name != 'pull_request'
    outputs:
      imageTag: ${{ steps.resolve-tag.outputs.imageTag }}
    steps:
      - uses: actions/checkout@v4

      - name: Resolve image tag
        id: resolve-tag
        run: |
          TAG="${{ inputs.imageTag }}"
          if [ -z "$TAG" ]; then
            TAG="${{ needs.version.outputs.semVer }}"
          fi
          echo "imageTag=$TAG" >> "$GITHUB_OUTPUT"

      - name: Log in to Azure (ACR)
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.ACR_CLIENT_ID }}
          tenant-id: ${{ secrets.ACR_TENANT_ID }}
          subscription-id: ${{ secrets.ACR_SUBSCRIPTION_ID }}

      - name: Log in to ACR
        run: az acr login --name "${{ secrets.ACR_HOSTNAME }}"

      - name: Build and push
        run: |
          IMAGE="${{ secrets.ACR_HOSTNAME }}/theprey/playfields-api:${{ steps.resolve-tag.outputs.imageTag }}"
          docker build -f src/PlayFields/HexMaster.ThePrey.PlayFields.Api/Dockerfile -t "$IMAGE" -t "${{ secrets.ACR_HOSTNAME }}/theprey/playfields-api:latest" .
          docker push "$IMAGE"
          docker push "${{ secrets.ACR_HOSTNAME }}/theprey/playfields-api:latest"

  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    needs: docker
    if: github.event_name != 'pull_request'
    steps:
      - uses: actions/checkout@v4

      - name: Log in to Azure
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_PROD_CLIENTID }}
          tenant-id: ${{ secrets.AZURE_PROD_TENANTID }}
          subscription-id: ${{ secrets.AZURE_PROD_SUBSCRIPTION }}

      - name: Read landing zone outputs
        id: lz
        run: |
          LZ=$(az deployment sub show --name theprey-lz-prod --query properties.outputs -o json)
          echo "acaEnvName=$(echo $LZ | jq -r '.containerAppsEnvironmentName.value')" >> "$GITHUB_OUTPUT"
          echo "appInsightsName=$(echo $LZ | jq -r '.appInsightsName.value')" >> "$GITHUB_OUTPUT"
          echo "appConfigEndpoint=$(echo $LZ | jq -r '.appConfigEndpoint.value')" >> "$GITHUB_OUTPUT"

      - name: Bicep build (lint)
        run: az bicep build --file infra/playfields/main.bicep

      - name: Deploy PlayFields
        run: |
          az deployment sub create \
            --name theprey-playfields-prod \
            --location westeurope \
            --template-file infra/playfields/main.bicep \
            --parameters \
              imageTag="${{ needs.docker.outputs.imageTag }}" \
              registryServer="${{ secrets.ACR_HOSTNAME }}" \
              registryUsername="${{ secrets.ACR_USERNAME }}" \
              registryPassword="${{ secrets.ACR_PASSWORD }}" \
              acaEnvironmentName="${{ steps.lz.outputs.acaEnvName }}" \
              appInsightsName="${{ steps.lz.outputs.appInsightsName }}" \
              appConfigEndpoint="${{ steps.lz.outputs.appConfigEndpoint }}"
